export declare type SidebarItem = {
  id: string;
  title: string;
  icon: string;
};

export declare type taskItem = {
  id?: string;
  listID: string;
  title: string;
  isDone: boolean;
  icon: string;
};
