export declare type SidebarItem = {
  id?: string;
  title: string;
  icon: string;
  number: number;
  index?: number;
};

export declare type taskItem = {
  id?: string;
  title: string;
  isDone: boolean;
  icon: string;
  onClick?: any;
};
